name: FE CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout FE Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build FE Docker Image
        id: build
        run: |
          TAG=$(echo $GITHUB_SHA | cut -c1-7)
          docker build -t image-fe:${TAG} -f Dockerfile .
          echo "TAG=${TAG}" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push FE Image to Docker Hub
        run: |
          docker push image-fe:${{ env.TAG }}

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Checkout FE Code
  #       uses: actions/checkout@v3

  #     - name: Deploy FE to VPS1
  #       run: |
  #         ssh user@<VPS1_IP> "docker stack deploy -c /path/to/docker-compose-fe.yml fe_stack"
